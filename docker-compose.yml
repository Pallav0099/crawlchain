services:
  searxng:
    image: searxng/searxng:latest
    container_name: searxng-crawler
    ports:
      - "4545:8080"
    volumes:
      - ./searxng:/etc/searxng
    environment:
      - SEARXNG_BASE_URL=http://localhost:4545/
    restart: unless-stopped
